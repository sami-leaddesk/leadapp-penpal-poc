/**
 * Initialize connection to parent LeadDesk application.
 *
 * @param {object} params
 * @param {string|RegExp} params.parentOrigin Overwrite default parent domain filter to allow custom domain as parent application. Usefull for e.g. development when running parent in localhost. Example: 'http://localhost'
 * @return {Promise.<ApiMethods>}
 */
export function LeadAppApi(params) {}
